import 'package:flutter/material.dart';

class FlutterLogoWidgets extends StatelessWidget {
  const FlutterLogoWidgets({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: FlutterLogo(),
      ),
    );
  }
}
